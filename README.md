# ConnectUs TIP Portal

## Set up environment:
		
	Install Dependencies 
		`npm install`

   To link cu-ui package locally for development:
      `npm link ../cu-ui` 

## Run:
  ### Development
     `npm start`

  ### Tests
     `npm run test`

  ### Production
     `npm run build`